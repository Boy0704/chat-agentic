{
  "name": "cek_stok",
  "description": "Check current stock levels for a product by name or SKU code",
  "parameters": {
    "type": "object",
    "properties": {
      "produk": {
        "type": "string",
        "description": "Product name or SKU code to look up"
      },
      "gudang": {
        "type": "string",
        "description": "Filter results by warehouse name (optional)"
      }
    },
    "required": ["produk"]
  }
}
